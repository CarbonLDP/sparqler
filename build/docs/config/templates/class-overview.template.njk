<h3>Overview</h3>
<div class="">
    <code class="typescript overview-code">
        {{ 'declare class' if doc.docType === 'class' else 'interface' }} {{ doc.name }}{{ doc.parsedTypeParams }}{% if doc.parsedHeritage.extends %} extends {{ doc.parsedHeritage.extends }}{% endif %}{% if doc.parsedHeritage.implements %} implements {{ doc.parsedHeritage.implements }}{% endif %} {<br>
        {% if doc.members %}
            <div class="overview-code__indent">
                {% for member in doc.members %}
                    {{ member.name }}{% if member.parsedParameters %}({% if member.parsedParameters.length > 0 %} {% endif %}{% for parameter in member.parsedParameters %}{{ parameter.name }}:{{ parameter.type }}{{ ', ' if not loop.last }}{% endfor %}{% if member.parsedParameters.length > 0 %} {% endif %}){% endif %}:{{ member.parsedReturnType }};<br>
                {% endfor %}
            </div>
        {% endif %}
        }
    </code>
</div>